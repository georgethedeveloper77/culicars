{
  "name": "culicars",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:api": "turbo build --filter=@culicars/api",
    "build:web": "turbo build --filter=@culicars/web",
    "build:admin": "turbo build --filter=@culicars/admin",
    "start:api": "node apps/api/dist/index.js",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}